/* const myRemove = require('./myRemove.js')

describe('Testando a função', () => {
    it('Retorna [1, 2, 4] quando receber 3', () => {
        const array = [1, 2, 3, 4]
        expect([1, 2, 4]).toEqual(myRemove(array, 3))
    });
    it('Não retorna [1, 2, 3, 4] quando receber 3', () => {
        const array = [1, 2, 3, 4]
        expect([1, 2, 4]).not.toEqual(array)
    });
    it('Retorna [1, 2, 3, 4] quando receber 3', () => {
        const array = [1, 2, 3, 4]
        expect([1, 2, 3, 4]).toEqual(myRemove(array, 5))
    });

}) */