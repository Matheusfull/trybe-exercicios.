/*  const myFizzBuzz = require('./myFizzBuzz.js');

describe('Testando o FiizBuzz', () => {
    it('se o número for divisível por 3 e por 5 então retorne FizzBuzz', () => {
        expect('fizzbuzz').toEqual(myFizzBuzz(15));
    });
    it('se o número for divisível por 3 fizz', () => {
        expect(myFizzBuzz(9)).toEqual('fizz');
    });
    it('se o número for divisível por 3 fizz', () => {
        expect(myFizzBuzz(10)).toEqual('buzz');
    });
    it('se o número não for divisível por 3 nem pro 5, retorna o próprio número', () => {
        expect(myFizzBuzz(11)).toEqual(11);
    });
    it('se não for um número, retornará false', () => {
        expect(myFizzBuzz('a')).toBe(false);
    });
})
 */

 